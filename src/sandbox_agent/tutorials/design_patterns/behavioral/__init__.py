# pragma: exclude file
"""

Behavioral patterns are about identifying common communication patterns between objects and realize these patterns.
It then describes two specific behavioral patterns:
Observer Pattern: Observer pattern defines a dependency between objects so that whenever an object changes its state, all its dependents are notified and updated automatically.
Strategy Pattern: Strategy pattern allows an algorithm's behavior to be selected at runtime.

"""
